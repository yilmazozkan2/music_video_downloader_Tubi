import 'package:flutter/material.dart';
class ProjectDecorations{
  static const EdgeInsets allPadding = EdgeInsets.all(20);
  static const EdgeInsets onlyTopPadding =   EdgeInsets.only(top: 20);
  static const EdgeInsets symetricPadding = EdgeInsets.symmetric(vertical: 15, horizontal: 0);
}